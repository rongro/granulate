export function getWeather(location) {
    // Call API to get weather details based on the location and return the details. Here we return mock data.
    return { temperature: 25, icon: 'https://cdn2.iconfinder.com/data/icons/weather-flat-14/64/weather02-128.png' };
}